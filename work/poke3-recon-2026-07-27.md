# Poke3 只读侦察记录

时间：2026-07-27 11:35 Asia/Shanghai

## 已确认

- Mac：Apple Silicon，macOS Darwin 25.5.0。
- USB 设备：Vendor `ONYX`，Product `SDM636-MTP`，VID `0x18D1`，PID `0x4F01`，USB serial `BE87E832`。
- 当前 USB 形态：Android 正常运行中的 MTP。
- ADB：`/opt/homebrew/bin/adb` 37.0.0，`adb devices -l` 为空。
- Fastboot：`/opt/homebrew/bin/fastboot` 37.0.0，`fastboot devices` 为空。
- 系统串口：没有出现典型 USB-UART 端口；现有 `/dev/cu.*` 包括 Bluetooth、DX05Pro、NothingEaropen、PiPods 和 Mac 自身 `debug-console`。

## 当前结论

`SDM636-MTP` 与 Poke3 用户描述相符，但精确型号、Android build、bootloader 状态、分区布局和加密/签名状态尚未由设备内部信息证明。

目前不能进行设备级读取，也没有任何刷写、擦除、解锁或持久化配置动作。

## 下一步前置动作

在 Poke3 上开启开发者选项中的 USB 调试，并接受 Mac 上弹出的 RSA 授权；保持设备正常开机并用当前数据线连接。授权后重新执行 ADB 只读查询，再决定是否需要接触 bootloader 或真正 UART。
